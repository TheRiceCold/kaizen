// import { options, utils } from '../constants/main.js'

export function setupNeovim() {
  // if (utils.dependencies(['lvim'])) { }
  // if (utils.dependencies(['codium'])) { }
  // if (utils.dependencies(['nvim'])) { }
  print('TODO: setup editor config colorschemes')
}
